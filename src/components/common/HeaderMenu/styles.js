import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    headerMenu: {
        height: '100%', 
        alignItems: 'center', 
        justifyContent: 'center',
        marginRight: 5,
        flexDirection: 'row'
    }
});
